[project]
name = "crypt-api-worker"
version = "0.1.0"
description = "Temporary self-destructing message API on Cloudflare Workers"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
  "workers-py",
  "workers-runtime-sdk"
]
